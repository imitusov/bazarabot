# Secrets — .env holds a full-access brokerage token.
# Ignored before it exists, deliberately: a credential committed once is
# committed forever, and the only remedy is revoking it at the broker.
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Runtime data — the live trade log and its backups belong on the server,
# not in version control.
data/
*.db
*.db-wal
*.db-shm
backups/

# The single-instance lock and its alert marker (spec §10, #22). They sit
# beside DB_PATH, so a developer whose DB_PATH is ./zarabot.db gets
# ./zarabot.db.instance-lock — which none of the patterns above match.
*.instance-lock
*.instance-lock.refused

# Research artefacts
sandbox/cache/
sandbox/models/
*.ipynb
*.ipynb_checkpoints/

# Agent scratch — worktrees a background task checks out under the repo. They
# are full working copies, so committing one would nest the tree inside itself.
.claude/worktrees/

# OS
.DS_Store
